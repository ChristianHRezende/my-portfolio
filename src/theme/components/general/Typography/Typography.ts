import { TypographyToken } from "antd/es/typography/style";
import { colors } from "../../../variables";

const Typography: Partial<TypographyToken> = {
  colorText: colors.colorSecondary,
  colorTextHeading: colors.colorSecondary,
};
export default Typography;
