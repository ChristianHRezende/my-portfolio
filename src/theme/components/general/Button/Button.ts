import { ButtonToken } from "antd/es/button/style/token";
import { colors } from "../../../variables";

const Button: Partial<ButtonToken> = {
  fontWeight: "bold",
  primaryColor: colors.background,
  textTextColor: colors.colorPrimary,
};
export default Button;
