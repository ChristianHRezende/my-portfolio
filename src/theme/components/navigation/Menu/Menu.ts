import { MenuToken } from "antd/es/menu/style";
import { colors } from "../../../variables";

const Menu: Partial<MenuToken> = {
  colorBgContainer: colors.backgroundGray,
  colorText: colors.colorPrimary,
  fontFamily: "Poppins",
  fontSize: 15,
  fontWeightStrong: 600,
};
export default Menu;
