import { AliasToken } from "antd/es/theme/internal";
import { colors as colorsVars } from "../variables";

const colors: Partial<AliasToken> = {
  colorPrimary: colorsVars.colorPrimary,
};
export default colors;
